---
name: threejs-runtime
description: Three.js and WebGPU runtime specifics for the 3D game kernel — the details of building the 3D runtime once the 3D kernel begins. Consult this skill whenever working with the Three.js/WebGPU runtime or wiring the editor's output to a 3D scene. Stub until the 3D kernel work starts; targets Three.js/WebGPU.
---

# threejs-runtime

The runtime skill for Three.js and WebGPU — the specifics of building the 3D game runtime. This is a stub: the 3D kernel (`kernel-3d`) has not begun, so no runtime knowledge has been earned yet. It exists so that dual-writes have a home the moment 3D work starts. Content is earned from real runtime sessions, never invented.

## Decisions

_None recorded yet. Stub until the 3D kernel begins._

## Gotchas

_None recorded yet. Stub until the 3D kernel begins._

## Contracts

_None recorded yet. Reference kernel-repo and `vendor/` Three.js/WebGPU doc file paths here; never paraphrase them as prose._
