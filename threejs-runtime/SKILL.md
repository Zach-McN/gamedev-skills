---
name: threejs-runtime
description: Three.js and WebGPU runtime specifics for the kernel's 3D side — the details of building the 3D runtime and viewport once the first 3D project begins. Consult this skill whenever working with the Three.js/WebGPU runtime or wiring the editor's output to a 3D scene. Stub until 3D work starts; targets Three.js/WebGPU.
---

# threejs-runtime

The runtime skill for Three.js and WebGPU — the specifics of building the 3D game runtime. This is a stub: the kernel's 3D side has not begun, so no runtime knowledge has been earned yet. There is one kernel for both dimensions (`editor-kernel` D38) — this skill holds what the 3D viewport and runtime learn, `phaser4-runtime` holds the 2D side, and `editor-kernel` holds what is shared. It exists so that dual-writes have a home the moment 3D work starts. Content is earned from real runtime sessions, never invented.

## Decisions

_None recorded yet. Stub until the first 3D project begins._

## Gotchas

_None recorded yet. Stub until the first 3D project begins._

## Contracts

_None recorded yet. Reference kernel-repo and `vendor/` Three.js/WebGPU doc file paths here; never paraphrase them as prose._
