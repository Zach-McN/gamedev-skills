# AGENTS.md — gamedev-skills

The instructions for this repo live in **[CLAUDE.md](CLAUDE.md)**. Read that file; there is
nothing here that is not there.

This is a pointer rather than a copy, on purpose. It arrived as a full duplicate and drifted
from the original immediately. In a library whose whole value is being the single place
knowledge lives, a second copy of the rules for editing it is the failure the library exists
to prevent, one level up.
